import React from 'react';
import { AppContainer } from '~/component/Layout/AppContainer';

interface Props {}

const RoleListView: React.FC<Props> = props => {
    return (
        <AppContainer>
            <div>RoleListView</div>
        </AppContainer>
    );
};

export default RoleListView;
